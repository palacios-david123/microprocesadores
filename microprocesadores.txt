#include <Keypad.h>



const byte ROWS
const byte COLS

byte rowPins[ROWS]

byte colPins[COLS]

Keypad keypad = Keypad( makeKeymap(keys), rowPins, colPins, ROWS, COLS );

char password;
char realpassword;
byte index;

void setup(){
  Serial.begin(96);
  pinMode(1,OUTPUT);
  pinMode(3,OUTPUT);
